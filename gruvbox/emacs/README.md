<h2 align="center">Deploying</h2>
<p align="center">Move the directory to your `.config` folder and the `.emacs` file in your home dir.</p>
<p align="center">for Vim Keybindings (Evil Mode), clone the repository in dir `~/.config/emacs.d/`</p>
